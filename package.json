{
  "name": "redd-2fa",
  "version": "1.0.0",
  "description": "ReDD 2FA — Minimalist, offline-only TOTP authenticator browser extension",
  "private": true,
  "license": "CC-BY-NC-ND-3.0",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}